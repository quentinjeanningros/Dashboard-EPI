class IdWidget {
    constructor(API, id, color, config) {
        this.state = {
        }
        this.id = id
        this.API = API
        this.color = color
        this.config = config
    }
}

export default IdWidget;